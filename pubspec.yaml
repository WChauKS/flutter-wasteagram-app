name: wasteagram
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  location: ^3.0.0
  image_picker: ^0.6.7
  cloud_firestore: ^0.13.7
  firebase_storage: ^3.1.6
  intl: ^0.16.1
  transparent_image: ^1.0.0
  sentry: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/sentry_dsn.txt